# .gitignore

# 🛑 1. ARQUIVOS DE AMBIENTE E SEGURANÇA
# O .env contém JWT_SECRET e DATABASE_URL. Nunca deve ser comitado.
.env

# 🛑 2. ARQUIVOS GERADOS/LOCAIS
# Banco de dados SQLite local (se usado como fallback)
database.sqlite

# Pasta temporária criada pelo Multer para uploads de CSV
/uploads

# 3. DEPENDÊNCIAS
# Pasta que contém todas as dependências baixadas
/node_modules/
package-lock.json 

# 4. CONFIGURAÇÕES DO VS CODE E OUTROS
/.vscode/
npm-debug.log*